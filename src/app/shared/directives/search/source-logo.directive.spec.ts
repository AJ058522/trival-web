import { SourceLogoDirective } from './source-logo.directive';

describe('SourceLogoDirective', () => {
  it('should create an instance', () => {
    const directive = new SourceLogoDirective();
    expect(directive).toBeTruthy();
  });
});
